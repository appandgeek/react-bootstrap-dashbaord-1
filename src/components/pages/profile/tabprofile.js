import React from 'react';

const tabprofile = (props) => {

    return (
        <div className="tab-pane active show" id="profile#rofile" role="tabpanel" aria-labelledby="profile-tab"><p>Profile section</p></div>
    );
}

export default tabprofile;