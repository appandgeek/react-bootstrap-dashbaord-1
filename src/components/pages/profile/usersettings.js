import React from 'react';

const usersettings = (props) => {

    return (
        <div className="tab-pane active show" id="profile#settings" role="tabpanel" aria-labelledby="settings-tab">
            <p>User setting section</p>
        </div>
    );

}

export default usersettings;